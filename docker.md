### Q1. 도커(Docker)란 무엇인가요?
**A:**
도커는 애플리케이션을 독립된 환경에서 실행할 수 있도록 만들어주는 컨테이너 기반의 플랫폼입니다. 애플리케이션과 해당 애플리케이션이 실행되기 위해 필요한 모든 것을 포함한 컨테이너를 제공하여, 일관된 환경에서 실행되도록 보장합니다.

---

### Q2. 도커의 주요 구성 요소는 무엇인가요?
**A:**
1. **이미지 (Image)**:
   - 컨테이너 실행에 필요한 모든 것을 포함하는 읽기 전용 템플릿.
   
2. **컨테이너 (Container)**:
   - 이미지의 실행 가능한 인스턴스로, 독립된 환경에서 애플리케이션을 실행.

3. **도커 파일 (Dockerfile)**:
   - 이미지를 생성하기 위한 스크립트 파일.

4. **도커 데몬 (Docker Daemon)**:
   - 컨테이너와 이미지를 관리하는 백그라운드 프로세스.

5. **도커 클라이언트 (Docker Client)**:
   - 사용자가 도커 명령을 실행하는 인터페이스.

6. **도커 허브 (Docker Hub)**:
   - 공개 이미지 저장소.

---

### Q3. 도커는 어떻게 작동하나요?
**A:**
도커는 리눅스의 네임스페이스(Namespaces)와 cgroups(Control Groups)를 활용하여 컨테이너를 구현합니다.

#### 1. **네임스페이스 (Namespaces)**:
- 시스템 리소스를 격리하여 컨테이너 간 간섭을 방지.
- 주요 네임스페이스:
  - **PID 네임스페이스**: 프로세스 ID 격리.
  - **NET 네임스페이스**: 네트워크 인터페이스 격리.
  - **MNT 네임스페이스**: 파일 시스템 마운트 격리.
  - **UTS 네임스페이스**: 호스트 이름 및 도메인 격리.

#### 2. **cgroups (Control Groups)**:
- CPU, 메모리, 네트워크 I/O 같은 리소스 사용량을 제한 및 관리.

#### 3. **Union 파일시스템 (UnionFS)**:
- 여러 계층으로 구성된 읽기/쓰기 가능한 파일시스템.
- 변경 사항만 기록하여 효율적으로 이미지를 관리.

#### 4. **가상 네트워크 (Virtual Networking)**:
- 각 컨테이너는 독립적인 네트워크 환경을 가집니다.

---

### Q4. 도커의 장점은 무엇인가요?
**A:**
1. **경량화**:
   - 전통적인 가상머신(VM)보다 빠르고 가벼움.

2. **이식성**:
   - "한 번 빌드, 어디서든 실행" 가능.

3. **효율성**:
   - 여러 컨테이너가 OS 커널을 공유하여 자원 낭비 최소화.

4. **자동화**:
   - CI/CD 파이프라인에 쉽게 통합 가능.

---

### Q5. 도커와 가상머신(VM)의 차이점은 무엇인가요?
**A:**
| 특징                | 도커 컨테이너                           | 가상 머신                              |
|---------------------|-----------------------------------------|---------------------------------------|
| 실행 속도            | 매우 빠름                               | 느림                                  |
| 크기                 | 경량 (몇 MB 수준)                       | 무겁고 용량 큼 (GB 수준)               |
| 자원 사용            | 효율적 (호스트 OS 커널 공유)             | 비효율적 (별도의 OS 필요)              |
| 격리 수준            | 프로세스 수준의 격리                    | 하드웨어 수준의 격리                   |

---

### Q6. 도커의 핵심 기술은 무엇인가요?
**A:**
1. **네임스페이스**를 통해 리소스를 격리.
2. **cgroups**를 활용한 자원 관리.
3. **UnionFS**로 효율적인 계층화된 이미지 관리.
4. **가상 네트워크**로 독립적인 네트워킹 제공.

