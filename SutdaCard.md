# SutdaCard equals 메서드 관련 질문과 답변

## 질문![KakaoTalk_20250122_152249886](https://github.com/user-attachments/assets/e373c651-c758-486b-9f8c-f7b1172fe0ed)

왜 이렇게 해야하나요?

## 답변
빨간색으로 표시된 부분(코드)은 `equals` 메서드의 구현에 중요한 조건들을 충족하기 위해 작성되었습니다. 하나씩 이유를 설명해 드릴게요:

---

1. **`obj instanceof SutdaCard`**  
   - 이 조건은 `obj` 객체가 `SutdaCard` 클래스의 인스턴스인지 확인합니다.  
   - `equals` 메서드에서 두 객체를 비교하려면 같은 클래스의 객체인지 확인하는 것이 필수입니다. 그렇지 않으면 타입 오류가 발생할 수 있습니다.  
   - 예를 들어, `SutdaCard`가 아닌 객체를 비교하려 하면 적절한 비교를 할 수 없기 때문에 이 조건이 필요합니다.

2. **`SutdaCard c = (SutdaCard) obj;`**  
   - `obj`가 `SutdaCard` 클래스의 인스턴스임이 확인되었으니, 안전하게 `SutdaCard` 타입으로 형변환(캐스팅)할 수 있습니다.  
   - 이렇게 형변환한 후 `num`과 `isKwang` 값을 비교할 수 있습니다.

3. **`return num == c.num && isKwang == c.isKwang;`**  
   - `num`과 `isKwang`은 `SutdaCard` 객체의 핵심 속성입니다. 두 객체가 동일하다고 판단하려면 이 두 속성이 모두 같아야 합니다.  
   - 따라서 `num`이 같은지(`num == c.num`), `isKwang` 값이 같은지(`isKwang == c.isKwang`) 확인하는 논리를 사용합니다.

4. **`return false;`**  
   - 만약 `obj`가 `SutdaCard`가 아니라면 `false`를 반환합니다. 이는 두 객체가 같지 않음을 나타냅니다.

---

### 결론:
빨간색 부분처럼 코드를 작성해야 `equals` 메서드가 `SutdaCard` 객체 간의 비교를 정확하고 안전하게 수행할 수 있습니다. 만약 이런 조건을 생략한다면 다음과 같은 문제가 발생할 수 있습니다:
- 잘못된 클래스 객체를 비교하려다 예외가 발생.
- `equals` 메서드가 두 객체를 올바르게 비교하지 못함.  

따라서 이러한 방식으로 구현하는 것이 올바른 객체 지향 프로그래밍 관행입니다. 😊

---

## 질문
왜 캐스팅해야 합니까? 인스턴스임을 이미 확인했다면서요.

## 답변
좋은 질문입니다! 사실 `instanceof`로 객체가 특정 클래스의 인스턴스인지 확인한 뒤에는 **캐스팅 없이도 접근할 수 있으면 더 편할 것**처럼 느껴질 수 있습니다. 그러나 자바의 설계상, `instanceof`로 타입 확인을 한 것과 **타입 캐스팅**을 수행하는 것은 별개의 작업입니다. 왜 그런지 자세히 설명드릴게요.

---

### 1. **`Object` 타입의 한계**
`equals` 메서드의 매개변수는 **`Object obj`** 타입으로 선언됩니다.  
자바의 모든 클래스는 `Object` 클래스를 상속하므로, `obj`가 실제로 어떤 객체이든 일단 `Object` 타입으로 취급됩니다.

- `Object` 타입으로 전달된 객체는 `Object` 클래스에 정의된 메서드와 속성만 사용할 수 있습니다.
- `SutdaCard`에 있는 **`num`**과 **`isKwang`** 속성은 `Object` 클래스에 정의되어 있지 않기 때문에 **직접 접근할 수 없습니다**.

따라서 `SutdaCard` 타입으로 **명시적 캐스팅**을 수행해야만 `SutdaCard` 클래스의 속성과 메서드에 접근할 수 있습니다.

---

### 2. **자바의 설계 철학 (타입 안정성)**
`instanceof`로 객체의 타입을 확인했더라도 자바는 타입 캐스팅이 필요합니다. 이는 자바가 강타입 언어(Strongly Typed Language)이기 때문입니다.

- 자바는 컴파일러가 타입 안정성을 철저히 확인하도록 설계되었습니다.
- `instanceof`는 런타임에서 확인되는 논리이므로, **컴파일 시점에는 여전히 `obj`가 `Object` 타입으로 간주됩니다.**
- 따라서 타입 캐스팅을 통해 **컴파일러에게 "이 객체는 안전하게 해당 타입으로 사용할 수 있다"고 알려주는 작업이 필요합니다.**

---

### 3. **캐스팅 없이 사용하려면?**
캐스팅 없이 사용하려면 `equals` 메서드의 매개변수를 애초에 `SutdaCard` 타입으로 선언하면 됩니다. 예를 들어:

```java
public boolean equals(SutdaCard obj) {
    return num == obj.num && isKwang == obj.isKwang;
}
```

이렇게 작성하면 명시적인 캐스팅이 필요 없습니다. 그러나 이 방법은 다음과 같은 단점이 있습니다:

- **재정의(Override)**: `equals` 메서드는 `Object` 클래스의 메서드를 재정의(Override)해야 합니다. 하지만 매개변수 타입이 `Object`가 아니면 **재정의가 아니게 되므로, 자바의 규칙을 따르지 않는 코드가 됩니다.**
- **호환성 문제**: `equals` 메서드는 자바의 표준 규칙에 따라 동작해야 하므로, 다른 코드와의 호환성이 떨어질 수 있습니다.

---

### 결론
- `instanceof`는 객체가 특정 클래스인지 확인만 해줍니다.
- `캐스팅`은 해당 타입의 속성과 메서드에 접근할 수 있도록 합니다.

이 두 가지는 서로 다른 역할을 수행하며, 자바에서 타입 안정성과 코드 명확성을 보장하기 위해 모두 필요합니다.
